# Pourō-GPT Re Final Context Pack — GPT-5.5 高 v1.1.1

Generated and patched: 2026-07-01

This package contains specification, design context, role prompts, QA gates, and an implementation roadmap for Pourō-GPT Re. It is a context/governance artifact; it is **not evidence that the current GitHub implementation matches these documents**.

## Structure

```text
docs/
  context/
  prompts/
  design/
  qa/
  implementation/
```

## Verification Boundaries

- Current repository, branch, PR, CI, deployment, and Figma state were not inspected as part of this patch.
- Physical iPhone, VoiceOver, Reduce Motion, offline, sound, and PWA standalone checks remain `NOT_RUN` unless separate evidence exists.
- External pricing, reviews, SDK capabilities, and platform constraints remain `UNVERIFIED` or `RESEARCH_REQUIRED` where stated.
- `NS-del346/Pouro-GPT` is protected and must not be modified.

## Required Owner Approval

Explicit owner approval is required before:

1. main merge
2. public production deploy
3. Figma final publish / re-LOCK

## Integrity and SHA-256

- `MANIFEST.json` records the files and hashes generated for this specific package.
- `SHA256SUMS.txt` was regenerated after the v1.1.1 modifications.
- The hashes are trustworthy only for the exact saved files in this generated package.
- Editing files after extraction, changing line endings, renaming content, or manually copying text from chat can change the bytes and therefore invalidate the recorded hashes.
- After any manual change or GitHub Web UI reconstruction, regenerate and recheck the hashes before treating them as evidence.

## Patch Record

See `PATCH_REPORT_JA.md` for the v1.1.1 change scope, unchanged areas, and remaining verification gaps.
